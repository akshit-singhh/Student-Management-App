<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.student_management_app">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Student_Management_App"
        tools:targetApi="31">

        <!-- Splash screen -->
        <activity
            android:name=".ui.login.SplashActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true" />

        <!-- Admin Dashboard screen -->
        <activity
            android:name=".ui.login.AdminDashboardActivity"
            android:exported="false" />

        <!-- Admin Profile screen -->
        <activity
            android:name=".ui.login.AdminProfileActivity"
            android:exported="false" />

        <!-- Student List screen -->
        <activity
            android:name=".ui.studentlist.StudentListActivity"
            android:exported="false" />

        <!-- Add/Edit Student screen -->
        <activity
            android:name=".ui.studentform.AddEditStudentActivity"
            android:exported="false" />

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>
</manifest>
